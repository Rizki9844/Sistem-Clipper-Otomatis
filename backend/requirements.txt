# ==============================================================
# AutoClipperPro — API Server Dependencies (Lightweight)
# ==============================================================
# This file is for the BACKEND API only (Heroku).
# Worker dependencies are in requirements-worker.txt
# ==============================================================

# ---- Core Framework ----
fastapi==0.109.2
uvicorn[standard]==0.27.1
pydantic==2.6.1
pydantic-settings==2.1.0

# ---- Database (MongoDB) ----
motor==3.3.2
beanie==1.25.0
pymongo==4.6.1

# ---- Task Queue (broker only, no worker) ----
celery[redis]==5.3.6
redis==5.0.1

# ---- Azure Services ----
azure-storage-blob==12.19.0
azure-identity==1.15.0

# ---- AI (Gemini — lightweight, no PyTorch needed) ----
google-generativeai==0.8.4

# ---- HTTP Client ----
httpx==0.27.0

# ---- Logging ----
structlog==24.1.0

# ---- Monitoring ----
sentry-sdk[fastapi,celery]==1.40.0

# ---- Utilities ----
python-dotenv==1.0.1
python-multipart==0.0.9

# ---- Security ----
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
